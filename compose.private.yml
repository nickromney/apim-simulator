services:
  smoke-runner:
    image: dhi.io/python:3.13-debian13-dev
    profiles:
      - smoke
    working_dir: /work
    entrypoint:
      - sh
      - -lc
    command:
      - tail -f /dev/null
    volumes:
      - .:/work:ro
    networks:
      - apim

networks:
  apim:
